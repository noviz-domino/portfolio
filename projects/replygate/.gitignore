# 비밀값 — 절대 커밋 금지
.env
*.key
secrets*
**/credentials*.json

# 대시보드용 구글 서비스 계정 키 (개인키가 들어 있다)
# 파일명을 바꿔도 걸리도록 넓게 잡는다
*service-account*.json
*service_account*.json
sa-key*.json
dashboard/*.json
dashboard/keys/

# n8n 에서 그대로 내려받은 파일 (API 키가 살아 있다)
# 저장소의 workflows/*.json 은 키를 자리표시자로 치환한 것이라 커밋해도 된다
ReplyGate*.json
*백업*.json
*backup*.json

# 재생성 가능한 캐시 (조항 임베딩 723KB, scripts/run_eval.py 가 다시 만든다)
eval/조항_임베딩_캐시.json

# 파이썬
__pycache__/
*.py[cod]
.venv/
venv/

# OS / 편집기
.DS_Store
Thumbs.db
.vscode/
.idea/
